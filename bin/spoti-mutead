#!/bin/sh

com="playerctl -p spotify"

while true
do  
  if { $com metadata | grep -q Bones && amixer get Master | grep -Fq '[on]'; }
  then
      $com volume 0

      while $com metadata | grep -q Bones
      do
	  sleep 2
      done

      $com volume 1
  fi
  sleep 5
done
